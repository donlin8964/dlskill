# dlskill 架构文档 v1.0

## 架构总览

dlskill 1.0 采用「1个主入口 + 9个专项子skill」的联邦架构，替代 v0.7 的单文件大而全模式。

```
dlskill/
├── SKILL.md              # 主入口 dls：路由 + 导航 + 新手引导
├── architecture.md       # 本文档
├── README.md             # 使用说明
├── VERSION               # 版本号
├── references/           # 共享知识库（所有子skill可引用）
│   ├── frameworks.md     # 五大框架 + 混搭公式 + 故事工具箱
│   ├── checklist.md      # 三层质检标准
│   ├── language.md       # 语言手册 + 黑话替换表
│   ├── platform.md       # 五大平台适配指南
│   └── iteration.md      # 爆款拆解模板
└── skills/               # 9个专项子skill
    ├── dls-write/        # 从零写稿（9步流水线）
    ├── dls-diagnose/     # 内容全维度诊断
    ├── dls-hook/         # 开头优化
    ├── dls-polish/       # 语言磨稿
    ├── dls-check/        # 发布前三层质检
    ├── dls-topic/        # 选题评估
    ├── dls-title/        # 标题生成
    ├── dls-platform/     # 平台适配
    └── dls-iteration/    # 爆款拆解
```

## 设计哲学（内化自 dbskill）

### 1. 消解优先于解答
v0.7 的问题是：不管用户什么需求，都走同一个9步流程。v1.0 借鉴 dbskill 的消解漏斗，先判断问题本身成不成立，再决定怎么解决。
- 体现在 dls-diagnose：先做真意核审查，核不对就不继续诊断执行层面
- 体现在 dls-topic：三关不过直接砍掉，不硬写
- 体现在 dls-hook：内容质量前置检查，内容不行就不优化开头

### 2. 每步停下来对话，不要闷头跑完
v0.7 的9步流程是一口气跑完的。v1.0 借鉴 dbskill 的「每做完一项就停下来把结论抛出来」，在关键节点（真意核/框架/标题）必须跟用户确认。

### 3. 核心机制审查
一段内容只能有一个核。所有内容都应该服务这个核。说了六件事等于什么都没说清楚。这是从 dbskill 的 dbs-resonate 共鸣诊断中内化的核心概念。

### 4. 标记式改稿，永不抹除原文
借鉴 dbskill 的 dbs-script-flow，改稿用 `~~删除线~~` + 🆕 + ⚠️ 三种符号，在原文上叠加标记，不用新版本替换旧版本。用户必须能一眼看出哪些是他写的、哪些是建议。

### 5. 公理/信条作为不可谈判原则
每个子skill都有自己的「核心哲学」section，列出不可谈判的信条。这是从 dbskill 的公理体系内化来的——不是建议，是原则。

### 6. 存档/恢复/报告的工作流闭环
每个子skill完成后都有明确的输出格式（诊断报告/改动清单/优化方案），并且完成后直接结束，由主入口负责下一步导航。

## 子skill职责边界

| 子skill | 输入 | 输出 | 不做什么 |
|---------|------|------|---------|
| dls-write | 选题/想法 | 完整文稿 | 不诊断已有稿子 |
| dls-diagnose | 已有文稿 | 诊断报告 + 标记式改稿 | 不从零写稿 |
| dls-hook | 已有文稿 | 开头优化方案（10-15条） | 不改正文其他部分 |
| dls-polish | 已有文稿 | 标记式语言改稿 + 改动清单 | 不改观点/结构 |
| dls-check | 已有文稿 | 质检报告（通过/不通过） | 不直接改稿 |
| dls-topic | 选题/想法 | 选题评估 + 改造建议 | 不写稿 |
| dls-title | 内容/选题 | 标题方案（10-15条）+ Top3 | 不写正文 |
| dls-platform | 已有文稿 | 平台适配建议 | 不直接改写全文 |
| dls-iteration | 爆款内容 | 拆解报告 + 可复制方法 | 不预测哪条会火 |

## 内容创作流水线

```
[dls-topic] 选题评估
     ↓ 三关通过
[dls-write] 从零写稿（9步流水线）
     ↓ 初稿完成
[dls-diagnose] 内容诊断（真意核 + 共鸣 + 逻辑）
     ↓ 诊断通过 / 改完
[dls-hook] 开头优化 ←→ [dls-polish] 语言磨稿（可交替进行）
     ↓
[dls-check] 发布前质检
     ↓ 质检通过
[dls-platform] 平台适配
     ↓
发布
     ↓
[dls-iteration] 爆款拆解（反向输入，随时可做，指导下一轮创作）
```

## 与 v0.7 的对比

| 维度 | v0.7 | v1.0 |
|------|------|------|
| 架构 | 单文件，9步流程 | 1主入口 + 9专项子skill |
| 路由 | 无，所有需求走同一个流程 | 按场景路由到专项skill |
| 诊断能力 | 弱（只有质检，没有真意核审查） | 强（真意核 + 五维度共鸣 + 逻辑延续） |
| 改稿方式 | 直接给新版本 | 标记式改稿，保留原文 |
| 用户交互 | 闷头跑完9步 | 关键节点确认，每步对话 |
| 开头优化 | 内嵌在写稿流程中，不深入 | 独立专项，内容质量前置检查 + 10-15条方案 |
| 语言打磨 | 内嵌在写稿流程中 | 独立专项，反黑话 + 金句 + 节奏 + 比喻 |
| 选题评估 | 写稿前的一个步骤 | 独立专项，三关筛选 + 改造建议 |
| 标题生成 | 写稿中的一个步骤 | 独立专项，六种类型 + 10-15条方案 |
| 平台适配 | references里的参考资料 | 独立专项，逐平台给出适配方案 |
| 爆款拆解 | references里的模板 | 独立专项，9维度拆解 + 仿写练习 |
| 共享知识 | 全部塞在一个文件里 | references/ 目录，子skill按需引用 |

## 设计决策记录

### 为什么拆成9个子skill而不是更少？
- 每个子skill单一职责，用户可以精准调用
- 每个子skill可以独立迭代，不影响其他
- 借鉴 dbskill 的31个子skill架构，但内容创作领域不需要那么多，9个是合理的最小完备集

### 为什么保留 references/ 而不是把知识全部分散到子skill里？
- 五大框架、黑话替换表、平台规则这些是共享知识，多个子skill都需要
- 放在 references/ 里统一维护，避免重复和不一致
- 子skill引用 references 而不是复制内容，保持DRY原则

### 为什么主入口不直接执行任务？
- 单一职责：主入口只做路由和导航
- 避免主入口变成另一个「大而全」
- 用户可以直接调用子skill（如果知道该用哪个），也可以通过主入口路由

### 标记式改稿的三种符号为什么这样设计？
- 借鉴 dbskill dbs-script-flow 的成熟设计
- `~~删除线~~`：视觉上最容易识别「这个要删」
- 🆕：明确标识「这是新增的」，和原文区分
- ⚠️：处理多轮反馈中的撤回，避免AI反复横跳弄丢用户的原文
