# scripts/

工具脚本目录。

## 脚本列表

| 脚本 | 用途 |
|------|------|
| `sync-version.sh` | 版本号同步，确保 VERSION 文件与 SKILL.md / README.md 版本一致 |

## 后续计划

- `release.sh`：版本发布脚本（自动更新版本号、生成更新日志、打 tag）
- `install.sh`：独立安装脚本（根目录的 install.sh 是发布用，这个是开发用）
- `lint.sh`：skill 文件格式检查
